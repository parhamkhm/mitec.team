node_modules/
.env
uploads/*
!uploads/.gitkeep
*.log

# Real admin credentials in plain text. Never commit — the repository copy of
# the account list is .env.example, which holds placeholders only.
ADMIN_CREDENTIALS.md
