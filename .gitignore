.DS_Store
Thumbs.db
node_modules/

# Impeccable live-mode working state: session journals, accept receipts and
# absolute machine paths. The design sidecar and live config are kept.
.impeccable/live/sessions/
.impeccable/live/accept-receipts/
.impeccable/live/roots.json
.impeccable/live/inject-journal.json

# Generated output from earlier Claude analysis sessions (the specs that came
# out of it live in docs/prompts/).
Claude outputs/

# Source PNGs of the Services card illustrations (about 1.3 MB each); the page
# uses only the WebP exports next to them (-768 / -1536).
project/public/images/services/*.png
